Running Test
# Finished Parsing
Type Checking Error
main.capable:14:0-0:
Protocol is not well-formed:
Auth causes error at:

because branch continutations differ:
  - login(Unit)
  .(recvFrom[Client] {pword(String).(sendTo[Service] {auth(Bool).End})})
  - cancel(Unit).(recvFrom[Client] {quit(Unit).End})

