type point = struct { x : Int; y : Int }

type either = union { left : String; right : Int}

func printLn(str:String) -> Unit
{
  print(str)
; print(toString('\n'))

}

func before() -> Unit
{
  local p = struct[point]{ x=1, y=2 }
; printLn(toString(get[x](p)))

; local p = set[x](p,3)
; printLn(toString(get[x](p)))

; local x : either = tag[left]("a")
; unit
}

func after() -> Unit
{
  local p = point(1,2)

; printLn(toString(get_x(p)))

; local p = set_x(p,3)
; printLn(toString(get_x(p)))

; local x : either = left("a")
; unit
}

main()
{

  before()
; after()
; unit

}
