Running Test
# Finished Parsing
Type Checking Error
main.capable:46:2-0:
Session is ill-typed, expecting an expression of type:
	(sendTo[Server] { adder(struct {x : Int; y : Int})
                  .(recvFrom[Server] {answer(Int).End})
| suber(struct {x : Int; y : Int}).(recvFrom[Server] {answer(Int).End})
| diver(struct {x : Int; y : Int})
  .(recvFrom[Server] {divByZero(Unit).End | answerE(Int).End})
| muler(struct {x : Int; y : Int}).(recvFrom[Server] {answer(Int).End}) })
but could not synthesis given type because of

Type Checking Error
main.capable:46:2-0:
Error merging local types: 
 (sendTo[Server] { adder(struct {x : Int; y : Int})
                  .(recvFrom[Server] {answer(Int).End}) })
(recvFrom[Server] {answer(Int).End})
(sendTo[Server] { diver(struct {x : Int; y : Int})
                  .(recvFrom[Server] { divByZero(Unit).End
                  | answerE(Int).End }) })
(sendTo[Server] { muler(struct {x : Int; y : Int})
                  .(recvFrom[Server] {answer(Int).End}) })
 
 errors where:
Types are wildly different.
.


