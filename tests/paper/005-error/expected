Running Test
# Finished Parsing
Type Checking Error
main.capable:23:2-0:
Error comparing local types:

 (recvFrom[Client] {msg(Int).(sendTo[Client] {msg(Int).End})})

and

 (sendTo[Client] {msg(Int).End})

because:

 Types are wildly different.

# Finished Parsing
Type Checking Error
main.capable:23:2-0:
Error comparing local types:

 (recvFrom[Client] {msg(Int).(sendTo[Client] {msg(Int).End})})

and

 (sendTo[Client] {msg(Int).End})

because:

 Types are wildly different.

